initSidebarItems({"struct":[["AbsoluteEvent","An AbsoluteEvent is an event that has an absolute time This is useful for apps that want to store events internally with absolute times and then quickly build an SMF file for saving etc..."],["SMFBuilder","An SMFBuilder can be used to create an SMF file.  This is done by adding tracks to the builder via `add_track` and then adding events to each track."]]});